# --- Server (Node/Express) ---
PORT=5000
MONGO_URI=mongodb://localhost:27017/udyamsaarthi
JWT_SECRET=change_this_to_a_long_random_string
ML_SERVICE_URL=http://localhost:8000

# --- ML Service (Python/FastAPI) ---
# Only needed once real LLM explanation calls are wired in (Module: AI Advisor)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Optional / future (per tech-stack doc, not required to run the MVP scaffold) ---
POSTGRES_URL=
REDIS_URL=
MAP_TILE_API_KEY=
